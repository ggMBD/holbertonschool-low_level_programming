#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @num: integer to test
 * Return: absolute value of num
 */

int _abs(int num)
{
	if (num >= 0)
		return (num);
	else
		return (-num);
}
